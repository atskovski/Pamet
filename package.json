{
  "name": "pamet",
  "version": "1.0.2",
  "private": true,
  "description": "Pamet — privacy-first personal health journal",
  "main": "secure-server.js",
  "scripts": {
    "start": "node secure-server.js",
    "check": "node --check server.js && node --check secure-server.js && node --check js/auth.js && node --check js/v1.0.2.js"
  },
  "engines": { "node": ">=20" },
  "dependencies": {
    "express": "^5.1.0",
    "mysql2": "^3.14.5",
    "stripe": "^18.5.0"
  }
}
